# =============================================================================
# Swiggy Nexus — environment reference
# =============================================================================
# Frontend: copy frontend/.env.example → frontend/.env.local
# Backend:  copy backend/.env.example  → backend/.env
#
# Production: use Render Blueprint — see docs/deploy-render.md
# =============================================================================
